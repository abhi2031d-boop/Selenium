image: python:3.12

stages:
  - test

before_script:
  - python --version
  - pip install -r requirements.txt

selenium_tests:
  stage: test
  tags:
    - selenium
  script:
    - pytest --html=reports/report.html
  artifacts:
    when: always
    paths:
      - reports/